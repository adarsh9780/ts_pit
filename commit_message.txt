feat(agent_v3): add response quality pipeline with validator/rewriter and bounded escalation

- add post-respond quality flow: `respond -> answer_validator -> (end | answer_rewriter | master)`
- enforce loop caps via state/config:
  - `max_answer_revision_attempts` (default 1)
  - `max_master_escalations_from_validation` (default 1)
- introduce structured `AnswerFeedback` and response-quality state fields
- add new modules:
  - `responding.py` (UX-focused final draft generation)
  - `validation.py` (hybrid deterministic + LLM quality checks)
  - `rewriting.py` (single-pass surgical rewrite)
- add explicit prompts:
  - `prompts/respond.yaml`
  - `prompts/answer_validator.yaml`
  - `prompts/answer_rewriter.yaml`
- update graph wiring and reset quality-loop state on new user questions
- add response-quality config loader in `backend/config.py` and defaults in `config.yaml`
- update SSE fallback extraction to ignore ephemeral internal node outputs and surface accepted final responses only

fixup:
- correct indentation in retry config fallback path in `backend/config.py`