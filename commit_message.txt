fix(agent-stream): prevent duplicate final output from fallback replay

- mark fallback token events with is_fallback flag
- dedupe fallback tokens in frontend when streamed text already matches
- preserve fallback behavior for non-streaming nodes so missing final answers are still filled
