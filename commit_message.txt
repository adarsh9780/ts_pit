fix(agent_v2): switch to correction-first retry policy for tool errors

- remove deterministic same-call replay on tool errors
- retry only for correctable error codes by routing back to LLM with error context
- enforce changed-input retries: block identical repeated tool-call signatures after a correctable error
- keep config-driven retry cap via `agent_v2.retries.max_tool_error_retries`
- add tests for:
  - retry guidance when model returns text after correctable error
  - blocking identical retry calls
  - allowing changed retry calls
  - cap exhaustion behavior
