fix(llm,structured-output): avoid callback serialization errors during report generation

- switch `with_structured_output(...)` calls in `backend/llm.py` to pass JSON schema dicts instead of Pydantic class objects
- normalize structured output results defensively (support dict or typed object) before field access
- prevent `LogStreamCallbackHandler` not-implemented serialization warnings for `ClusterSummaryOutput`/`ArticleAnalysisOutput`
- keep existing summary/analysis behavior unchanged while removing noisy runtime callback errors

fix(news-api): normalize article URL fields so frontend titles become clickable links

- update `/news/{isin}` payload mapping to detect URL-like article columns (`url`, `article_url`, `art_url`, `link`, `news_url`, `article_link`)
- emit canonical `url` and `article_url` fields whenever any URL-like source column is present
- ensure alert news cards can render title anchors consistently without requiring strict config column names
