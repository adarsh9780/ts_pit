feat(refactor): inline artifacts and configure report storage

- Moved `artifacts/` from project root to `backend/src/ts_pit/artifacts/` to bundle critical data with the package.
- Configured reports to be stored in `backend/src/ts_pit/data/reports/{session_id}/`.
- Updated `backend/src/ts_pit/reporting.py` `REPORTS_ROOT` to point to the new data directory.
- Updated `backend/src/ts_pit/agent_v3/tools.py` to:
  - Load `DB_SCHEMA_REFERENCE.yaml` from package resources.
  - Set `PROJECT_ROOT` to the package root.
  - Allow writing markdown reports to `data/reports/`.
- Updated `backend/config.yaml` `agent.filesystem.allowed_dirs` to include `data/reports` and increased default `max_depth`.
- Updated `execution.yaml` prompt example to reflect new artifact path.
