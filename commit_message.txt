feat(agent-ui): add collapsible tool traces and LaTeX rendering in chat

- stream richer tool telemetry from backend SSE (`tool_start` input preview, `tool_end` output preview, duration)
- keep artifact-created event behavior intact
- upgrade chat state to track per-tool execution metadata (id, status, input/output, duration)
- replace flat tool badges with collapsible per-tool sections showing structured input/output
- improve tool trace readability with dedicated styling and status metadata
- add markdown LaTeX support via `marked-katex-extension` + `katex`
- update frontend dependencies and lockfile
- verify backend compile and frontend production build