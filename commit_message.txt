feat(agent_v2): add no-data SQL recovery loop with bounded LLM-guided retries

- detect recoverable no-data case when execute_sql returns `ok=true` with `meta.row_count=0`
- route back to LLM with guidance to revise date/time filters before concluding no data
- enforce changed-query retries by blocking identical SQL signature repeats
- reuse existing retry cap from `agent_v2.retries.max_tool_error_retries`
- add tests for no-data retry trigger, identical-query blocking, and changed-query acceptance
