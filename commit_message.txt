fix(agent_v2): retry failed tool calls directly instead of re-prompting model text

- replace retry route from `retry_after_tool_error -> agent` with deterministic tool replay
- add failed-call reconstruction from latest tool error + original tool_call id
- retry node now emits a new AI tool_call for the same tool/args and routes to `tools`
- preserve config-driven retry cap via `agent_v2.retries.max_tool_error_retries`
- add tests to validate retry route and tool-call re-dispatch behavior
