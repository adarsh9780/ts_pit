fix(agent_v3): make execution/correction structured outputs Azure-strict safe

Fixes VDI runtime error:
- Invalid schema for response_format 'ProposedToolArgs'
- Azure strict mode rejects free-form object fields in structured output models.

Changes:
- backend/agent_v3/execution.py
  - ProposedToolArgs now uses `tool_args_json: str` (instead of dict tool_args)
  - add JSON parser helper `_parse_tool_args_json`
  - parse model output into dict server-side before tool execution
- backend/agent_v3/correction.py
  - CorrectedArgs now uses `tool_args_json: str`
  - add JSON parser helper `_parse_tool_args_json`
  - parse model output into dict server-side before applying corrections
- backend/agent_v3/prompts/execution.yaml
  - update output contract to require `tool_args_json` JSON string

Result:
- removes Azure strict response_format schema errors in executioner/correction nodes
- preserves dynamic tool args with deterministic server-side parsing
