feat(agent-chat): paginate history and add load-older UX

- add backend pagination to GET /agent/history/{session_id} with limit/offset
- return pagination metadata (has_more, next_offset, total)
- load latest 10 messages on panel open
- add "Load older messages" button to fetch next 10 messages
- avoid auto-scroll to bottom when prepending older history
- update frontend API service to pass history query params
