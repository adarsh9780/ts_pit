test(agent_v2): add execute_sql coverage across all core tables and safety paths

- add isolated temp-DB test suite for `backend.agent_v2.tools.execute_sql`
- validate successful SELECT reads for alerts, articles, prices, article_themes, prices_hourly
- verify logical-column rewrite behavior (`SELECT id, ticker FROM alerts`)
- assert read-only enforcement for non-SELECT SQL
- assert DB error path and schema hint for invalid column queries
