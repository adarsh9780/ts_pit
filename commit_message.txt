feat(agent_v2): add planner-router agent, generic filesystem tools, and robust python runtime manager

- implement planner -> context-loader -> direct/agent routing flow in agent_v2 graph
- add dynamic per-turn tool gating via TOOL_REGISTRY + active_tool_names
- replace KB-specific tools with generic file tools: list_files/read_file/write_file
- enforce config-driven filesystem policy (allowed_dirs, depth, read/write extensions, max bytes)
- keep report generation tool available in v2 (`generate_current_alert_report`)
- add web search tooling split (`search_web`, `search_web_news`, optional `scrape_websites`)
- integrate `execute_python` using local `safe-py-runner` package
- scaffold reusable `libs/safe_py_runner` package (policy/runner/worker + docs + tests)
- add cross-platform runtime resolver for python exec (Windows/Linux/macOS interpreter paths)
- add structured runtime config: venv_manager, base_python_executable, interpreter discovery candidates
- add startup/runtime import validation (`required_imports`) and package bootstrap/repair flow
- add setup utility `scripts/setup_safe_py_runner_env.py` and health check `scripts/check_safe_py_runner_env.py`
- update config and README for isolated agent runtime and diagnostics workflow
