fix(impact): handle non-integer article ids in staged update path

- change temp update table id type from INTEGER to TEXT to avoid sqlite datatype mismatch
- compare target article ids using `CAST(... AS TEXT)` in set-based update and diff checks
- preserve optimized write strategy while supporting mixed id representations across datasets
