agent_v3: force deterministic alert baseline, surface linked alerts explicitly, and prevent duplicate analysis reruns

agent_v3: add risk-based one-turn clarification flow with explainable options and fallback defaults

agent_v3: tighten deterministic alert step ordering, add explainable clarification routing, refine news queries (LLM+fallback), set DDGS backend order, and improve chat history rendering width/perf
