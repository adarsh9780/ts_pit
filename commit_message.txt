refactor(agent_v2): simplify safe_py_runner runtime config to manual venv path model

- replace `agent_v2.python_exec` with `agent_v2.safe_py_runner`
- remove auto-creation/install manager options (`venv_manager`, `base_python_executable`, `packages`, etc.)
- require `safe_py_runner.venv_path` to point to runner Python executable
- simplify runtime validation to path + required import checks only
- update agent v2 runtime consumers (`main.py`, `tools.py`, diagnostics script)
- delete `scripts/setup_safe_py_runner_env.py`
- update docs (`README.md`, `libs/safe_py_runner/README.md`)
- remove backward-compat config alias, enforcing single config path