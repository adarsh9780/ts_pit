fix(agent_v2): make SQL logical-column rewrite table-aware to prevent wrong price fields

- replace global first-match logical->physical rewrite with query-scoped mapping
- detect tables referenced in FROM/JOIN and resolve only unambiguous column mappings
- prevent cross-table collisions (e.g., prices.close vs prices_hourly.close)
- add regression test ensuring `SELECT date, close FROM prices_hourly` returns numeric close values
