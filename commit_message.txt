fix(agent-v3,prompts): enforce utf-8 prompt loading and remove non-ascii quote in respond prompt

- load prompt yaml files with explicit `encoding=\"utf-8\"` to avoid Windows cp1252 decode failures
- replace smart quote in `respond.yaml` with ASCII quote for cross-platform safety
- prevent `UnicodeDecodeError: 'charmap' codec can't decode byte 0x9d` during respond node prompt load
