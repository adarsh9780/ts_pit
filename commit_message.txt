perf(impact): vectorize impact-score calculation and remove per-row SQL loop

- replace per-article `iterrows` + SQL queries with vectorized ticker-batch computation
- load hourly prices once per ticker and compute baseline/event metrics with NumPy arrays
- use prefix sums + `searchsorted` for fast 10-day window std-dev and event-candle lookup
- keep existing scoring thresholds/labels and update semantics unchanged
- increase pandas chaining usage with `.assign()/.dropna()/.query()` pipeline
