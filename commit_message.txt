Add Excalidraw diagram for agent_v3 and create langgraph configuration

- Added a new Excalidraw file `agent_v3.excalidraw` containing a visual representation of the agent workflow.
- Created `langgraph.json` to define dependencies and graph configuration for the agent workflow.