feat(agent_v2): auto-retry after tool errors with config-driven retry cap

- add `agent_v2.retries.max_tool_error_retries` to config (default: 1)
- add config getter `get_agent_v2_retry_config()` with validation/clamping
- update graph continuation logic to detect latest `{"ok": false}` tool payloads
- inject retry guidance message and loop back to agent when retries remain
- stop retrying once configured cap is exhausted
- add unit tests for retry routing behavior after tool errors
